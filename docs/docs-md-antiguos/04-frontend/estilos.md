> ⚠️ **ARCHIVADO — no es fuente de verdad.**
> Este documento describe el sistema tal como era antes de la reorganizacion de `docs/`.
> Puede citar MariaDB, MongoDB, EMQX/MQTT o rutas que ya no existen. Se conserva por su
> valor historico. Para el estado actual, ver el `README.md` de la raiz.

# Frontend - Estilos (tecnico)

## Estilos globales

- Bootstrap: importado en frontend/src/main.js
- Tema principal: frontend/src/scss/theme.scss

## Variables SCSS

- frontend/src/scss/_variables.scss
- frontend/src/scss/_bootstrap-variables.scss

## Convenciones

- Menu lateral en estilo "cajas" (admin-menu, menu-section).
- Colores base definidos por variables CSS (ver theme.scss).

